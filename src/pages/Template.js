import React from "react";
import Layout from "../components/Layout";

const Template = ({ props }) => {
  return (
    <Layout navbar={true} footer={true} white={true}>
      Content Here
    </Layout>
  );
};

export default Template;
